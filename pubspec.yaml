name: adaptilearn_africa
description: P5 English Learning App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Add these for next steps:
  url_launcher: ^6.1.0  # For WhatsApp integration
  shared_preferences: ^2.2.0  # For saving progress

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
